/**
 * Re-export Pronote Connect screen for (home) tab navigation.
 *
 * This allows navigating to pronote-connect from child detail
 * without crossing tab boundaries (which breaks back navigation).
 */

export { default } from '../(profile)/pronote-connect';
