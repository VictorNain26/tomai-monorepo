# pnpm configuration for Expo SDK 56 + React Native monorepo
# node-linker=hoisted required for React Native to resolve native modules
# Native build-script allowlist lives in pnpm-workspace.yaml (allowBuilds) since pnpm 11.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
